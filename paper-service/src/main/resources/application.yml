micronaut:
  ssl:
    port: 8090
    enabled: true
    buildSelfSigned: true

datasources:
  default:
    url: jdbc:postgresql://localhost/paper-db
    username: postgres
    password: password
    driverClassName: org.postgresql.Driver