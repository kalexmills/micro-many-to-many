micronaut:
  server:
    port: 8090

datasources:
  default:
    url: jdbc:postgresql://localhost/paper-db
    username: postgres
    password: password
    driverClassName: org.postgresql.Driver